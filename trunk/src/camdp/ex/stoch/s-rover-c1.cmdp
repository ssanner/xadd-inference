cvariables (x)
min-values (-500000)
max-values (500000)
bvariables (tp)
nvariables (n)
ivariables ()
avariables (a)

action move (-10 < =a < =10)

tp'([x>=30]
	  ([1.0])
	  (tp
		  ([1.0])
		  ([0.0])))
	
x' ([x + a + n])

noise

n ([n <=5]
	([n>=-5]
		([legal])
		([illegal]))
	([illegal]))
	
reward (tp'
         (tp
           ([-1])
           ([10]))
         ([-1]))

endaction

discount 1.000000
iterations 8
